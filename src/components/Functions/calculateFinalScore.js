import PropTypes from "prop-types";

export default function calculateFinalScore(player) {
let finalScore = 0;

return finalScore;
}

calculateFinalScore.propTypes = {
  player: PropTypes.func.isRequired,
};